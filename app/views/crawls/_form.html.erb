<%= form_for @crawl, :html => { :class => "form-horizontal crawl" } do |f| %>

  <% if @crawl.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@crawl.errors.count, "error") %> prohibited this crawl from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @crawl.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :url, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :url, :class => 'form-control' %>
    </div>
    <%=f.error_span(:url) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                crawls_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
